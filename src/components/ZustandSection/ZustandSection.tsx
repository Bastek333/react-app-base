import ZustandButtons from "../ZustandButtons/ZustandButtons";
import ZustandCounter from "../ZustandCounter/ZustandCounter";

const ZustandSection = () => {
	return (
		<section>
			<h3>Zustand count button test</h3>
			<ZustandCounter />
			<ZustandButtons />
		</section>
	);
};

export default ZustandSection;
